$(document).ready(function() {});

//grab user input and store into new object, into friends array
CHECK;
//create function that loops through each friend
//create function that takes the value of each survey question and compares with next user
//whichever two users have the least difference in score, they are most compatible
// function that will pop up a small box, showing user they are most compatible with
